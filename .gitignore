# Environment variables (secrets - never commit)
.env
.env.local
.env.*
!.env.example
# NOTE: .env.example is NOT ignored - it's committed as a template
backend/.env

# (no root-level local docs anymore — all docs moved to docs/)

# Backend - Python
.venv/
.venv*/
backend/venv/
backend/venv*/
**/__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
backend/dist/
backend/build/

# Frontend - Node
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/dist/
frontend/.turbo/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# AI assistant working directories and config (local — not shared to GitHub)
.claude/
.opencode/
opencode.json
/AGENTS.md

# Tooling (local dev artifacts)
.impeccable/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Database
*.db
*.sqlite
*.sqlite3

# Runtime artifacts (logs, DBs, state files)
backend/runtime/

# Data Files
backend/data/items/
backend/data/*.csv
backend/data/*.json
!backend/data/historical_prices_template.json
!backend/data/historical_prices_template.csv

# Price archive (lives on data-archive branch, not main)
price-archive/

# ML model artifacts (regenerated on each retrain)
backend/models/saved_models/*.txt
backend/models/saved_models/*.pkl
backend/models/saved_models/bias_corrections.json
backend/models/saved_models/engineered_data.parquet

# Generated parquet cache
backend/data/*.parquet

# OS
Thumbs.db
